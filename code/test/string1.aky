int main() {
  // access using a[0] bug 
  // this should be a stack string ok
  let a = ['g','a','b',0 as char];
  // a[0] += 1;
  print "%s\n", a[0];
  let t = c"s";
  print "%s:%s\n", typeof(a), typeof(t);
  let t0 = "s";
  print "%s\n", typeof(t0);
  print "%s\n", t0 + t0;
  loop 4 { t0 = t0 + t0; }
  print "%s\n", t0;
}
