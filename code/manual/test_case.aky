
// enak ada makanan nih
enum Makanan {
  Ayam,
  Burger,
  Sapi,
  Daging
}

int main() {
  char *s = "buat";
  let sarapan = Makanan.Ayam;
  print "%s is %d\n", Makanan[sarapan], sarapan;
  sarapan++;
  print "%s is %d\n", Makanan[sarapan], sarapan;
  sarapan = Daging;
  switch (sarapan) {
    case Ayam, Daging:
      print "Ayam ato ngak daging!\n";
    leak case Burger:
      print "Yeay!\n";
    case Sapi:
      print "Huh!\n";
  }
  let nama = input("Nama: ");
  print "%s\n", nama;
  free nama;
  loop 10 { free nama; } // owh C ngaceng jir krn auto set null
  print "anjay aman bosku!\n";
}
