define hydrogren as int main();
define inta, intaa, intb as int;

inta zero0 = 0;
intaa zero1 = 1; 
intb zero2 = 2;

int main() {
  printf "%s", "Semua di dunia ini adalah objek\n";
  printf "Expected zero0 0, got %d", zero0;
  printf "Expected zero1 1, got %d", zero1;
  printf "Expected zero2 2, got %d", zero2;
  return 0;
}
