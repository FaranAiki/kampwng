# Alkyl Programming Language

A simple programming language written using LLVM-IR.

Core feature is the high-level and simplicity of C, combined with C++'s object oriented programming 
with is-a has-a feature.

Todo implemented:
* threading { }
